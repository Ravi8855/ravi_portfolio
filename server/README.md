Ravi Portfolio Server

1. Copy `.env.example` to `.env` and fill AWS credentials + S3 bucket name.
2. npm install
3. npm run dev (for development)
4. Ensure CORS allows your frontend origin (currently open)
